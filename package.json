{
  "name": "grides-react-app-rename",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://fast-falls-16836-fe68d5962d41.herokuapp.com",
  "scripts": {
    "start": "node server.js",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "server": "your-server-command-here",
    "client": "react-scripts start",
    "deploy": "npm run build && npm run server"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-scripts": "^5.0.1"
  },
  "dependencies": {
    "express": "^4.18.3"
  }
}
